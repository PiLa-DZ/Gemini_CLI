# =============================================================
## How to manage background processes
# -------------------------------------------------------------
# start long-running tasks
Start the React dev server in the background.
# Gemini will run the command (e.g., `npm run dev`) and detach it.
# -------------------------------------------------------------
(Ctrl+B) # Show or Close
(Ctrl+L) # List
(Ctrl+K) # Kill
